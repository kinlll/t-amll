<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kinl.tmall.dao.SysAdminMapper" >
    <resultMap id="BaseResultMap" type="com.kinl.tmall.pojo.SysAdmin" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="salt" property="salt" jdbcType="VARCHAR" />
    </resultMap>

    <select id="findByName" parameterType="String" resultType="SysAdmin">
        select * from sys_admin where name=#{name}
    </select>

    <select id="findPermissionById" parameterType="Integer" resultType="String">
        SELECT permission_name FROM sys_permissions WHERE id IN(
            SELECT DISTINCT permission_id FROM sys_role_permissions WHERE role_id IN(
                (SELECT role_id FROM sys_admin_role WHERE admin_id=#{id})))
    </select>

</mapper>